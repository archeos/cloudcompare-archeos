--- a/qCC/CMakeLists.txt
+++ b/qCC/CMakeLists.txt
@@ -122,7 +122,7 @@ install_ext( FILES ${CC_FBO_SOURCE_DIR}/
 install_ext( FILES ${CC_FBO_SOURCE_DIR}/bilateral/bilateral.vert ${qCC_dest_release}/shaders ${qCC_dest_debug}/shaders )
 
 # Auxiliary files
-set( auxFiles bin_other/history.txt bin_other/license.txt )
+# set( auxFiles bin_other/history.txt bin_other/license.txt )
 
 if( WIN32 )
 	# Export Qt dlls & plugins
@@ -134,6 +134,6 @@ if( WIN32 )
 endif()
 
 # Install auxiliary files
-foreach( filename ${auxFiles} )
-	install_ext( FILES ${filename} ${qCC_dest_release} ${qCC_dest_debug} )
-endforeach()
+# foreach( filename ${auxFiles} )
+#	install_ext( FILES ${filename} ${qCC_dest_aux} ${qCC_dest_debug} )
+# endforeach()
--- a/ccViewer/CMakeLists.txt
+++ b/ccViewer/CMakeLists.txt
@@ -102,6 +102,6 @@ if( WIN32 )
 endif()
 
 # Install auxiliary files
-foreach( filename ${auxFiles} )
-	install_ext( FILES ${filename} ${ccViewer_dest_release} ${ccViewer_dest_debug} )
-endforeach()
+#foreach( filename ${auxFiles} )
+#	install_ext( FILES ${filename} ${ccViewer_dest_release} ${ccViewer_dest_debug} )
+#endforeach()
